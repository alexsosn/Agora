# Plugins

This directory contains Agora plugin integrations.

A plugin may define one or more MCP servers, scholarly skills/instructions, launch or adaptation logic, and plugin-specific metadata. Third-party upstream implementations should normally be referenced rather than vendored.

The plugin metadata model itself is Phase 1 work. Do not invent per-plugin schemas here before the canonical registry is defined.

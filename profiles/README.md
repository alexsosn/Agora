# Profiles

Optional curated bundles of Agora plugins and provider-specific resources live here.

Profiles are convenience selections, not compatibility layers. Agora does not require a profile reproducing the historical `mcp-demo` workshop setup.

A concrete profile schema should be introduced only if and when profiles become useful later in the implementation plan.

# Registry

This directory will contain Agora's canonical machine-readable marketplace metadata and schemas.

Phase 1 defines the registry schema and initial plugin records. Client-specific marketplace manifests must be derived from this canonical data rather than maintained here as parallel sources of truth.
